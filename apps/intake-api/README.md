# Intake API

Private internal HTTP service for security-critical signed-event verification and atomic PostgreSQL persistence. It is reached through WF-02 and has no host-published port.

See [Signed Unified Ingestion](../../docs/signed-ingestion.md).
