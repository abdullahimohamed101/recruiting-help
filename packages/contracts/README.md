# Contracts

Strict Zod schemas and inferred TypeScript types for raw events, source-specific metadata, opportunity candidates, and durable state values.

See [Database and Contracts](../../docs/database.md).
